{"prompt": "PackML FSM for line1: LoadRecipe is koshei-governed via ot-recipe-stage-activate; Start/Hold/Abort are field-driven. stateNode line1.stateCurrent, unit line1."}
{"prompt": "Author a PackML unit FSM for line1 with states Idle(4), Execute(6), Held(11), Complete(17), Aborted(9). The operator-initiated LoadRecipe command in Idle must be koshei-governed and drive the ot-recipe-stage-activate workflow; Start, Hold, and Abort are reactive field-driven transitions with no action. stateNode is line1.stateCurrent."}
{"prompt": "I need a governed FSM for line1's PackML unit. Only the recipe-load step (command LoadRecipe, staying in Idle) should be driven by koshei via the ot-recipe-stage-activate workflow. Everything else (Start into Execute, Hold, Abort) is field/PLC-driven, no koshei action. Use stateNode line1.stateCurrent and unit line1."}
