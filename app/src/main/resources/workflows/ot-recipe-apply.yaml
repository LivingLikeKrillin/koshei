name: ot-recipe-apply
steps:
  - { block: db.read, version: "1.0.0" }
  - { block: db.upsert, version: "1.2.0", params: { table: target_rows } }
  - { block: notify.email, version: "1.0.0" }
  - { block: transform.map, version: "1.0.0" }
  - { block: actuate, version: "1.0.0" }
